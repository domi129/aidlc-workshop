{
  "name": "aidlc-workshop",
  "version": "1.0.0",
  "description": "Table Order Service - AIDLC Workshop",
  "scripts": {
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "setup:db": "node scripts/setup-dynamodb.js",
    "start:all": "npm run docker:up && timeout /t 5 && npm run setup:db"
  },
  "devDependencies": {
    "aws-sdk": "^2.1400.0"
  },
  "dependencies": {
    "axios": "^1.13.5"
  }
}
